{
  "name": "hardhat",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bot": "npx hardhat run --network localhost src/main.ts",
    "swap": "npx hardhat run --network localhost src/swap.test.ts",
    "test": "npx hardhat test",
    "coverage": "npx hardhat coverage",
    "deploy": "npx hardhat run --network localhost scripts/deployArbitrage.ts",
    "node:deploy:bot": "concurrently 'npx hardhat node' 'sleep 12 && npm run deploy' 'sleep 20 && npm run start'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/contracts": "^4.4.1",
    "@uniswap/sdk": "^3.0.3",
    "@uniswap/v2-core": "^1.0.1",
    "@uniswap/v2-periphery": "^1.1.0-beta.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "chai": "^4.3.6",
    "dotenv": "^10.0.0",
    "hardhat": "^2.12.2",
    "concurrently": "^7.5.0"
  }
}
